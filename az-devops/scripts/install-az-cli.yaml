steps:
    -   bash: |
            # curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash
            apt-get update
            apt-get install azure-cli
            echo "Azure CLI version is::"
            az --version
        #bash: az --version
        #bash: curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash
        displayName: 'Install Azure CLI version'
        